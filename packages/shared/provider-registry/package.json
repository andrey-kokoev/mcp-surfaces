{
  "name": "@narada-core/provider-registry",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "type": "module",
  "description": "Typed, policy-neutral provider/model capability registry loading and resolution.",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "test": "pnpm run test:bun",
    "test:bun": "pnpm build && bun dist/test/provider-registry.test.js",
    "test:node": "pnpm build && node dist/test/provider-registry.test.js"
  },
  "types": "./dist/src/main.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/main.d.ts",
      "default": "./dist/src/main.js"
    }
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}
